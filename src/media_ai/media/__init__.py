"""Local media helpers: Pillow placeholder rendering and ffmpeg clip/concat ops.

Used by the offline mock provider and by ``media-ai concat`` (which joins clips on
the local machine regardless of the generation provider).
"""
